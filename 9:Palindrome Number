bool isPalindrome(int x) {
    long long rem;
    long long rev=0;
    int o=x;
    if(x<0){
        return false;
    }
    while(x!=0){
    rem=x%10;
    rev=rev*10+rem;
    x=x/10;}
    if(o==rev)
    {return true;}
    return false;
}
